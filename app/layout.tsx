import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'IFI Chatbot - International Financial Institutions',
  description: 'AI-powered chatbot for International Financial Institutions with document Q&A capabilities',
  keywords: ['chatbot', 'AI', 'financial', 'institutions', 'document', 'Q&A'],
  authors: [{ name: 'IFI Team' }],
}

export const viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="light">
      <body className={`${inter.className} antialiased`}>
        <div className="min-h-screen bg-gradient-to-br from-slate-50 to-blue-50">
          {children}
        </div>
      </body>
    </html>
  )
}
